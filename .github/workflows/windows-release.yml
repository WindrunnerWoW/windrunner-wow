name: Windows Release

run-name: Release ${{ inputs.release_name }} from ${{ github.ref_name }}

on:
  workflow_dispatch:
    inputs:
      release_name:
        description: Name shown on the GitHub Release
        required: true
        type: string

concurrency:
  group: windows-release
  cancel-in-progress: false

env:
  RELEASE_VERSION: '0.1'
  BUILD_TYPE: Release
  _CL_: /MP1
  VCPKG_ROOT: C:/vcpkg
  VCPKG_DEFAULT_TRIPLET: x64-windows
  INSTALL_DIR: ${{ github.workspace }}/install

jobs:
  release:
    runs-on: windows-2022
    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Validate selected branch and commit
        shell: pwsh
        run: |
          if ($env:GITHUB_REF_TYPE -ne 'branch') { throw 'Select a branch for the release' }
          $checkoutSha = git rev-parse HEAD
          if ($LASTEXITCODE -ne 0 -or $checkoutSha -ne $env:GITHUB_SHA) {
            throw "Checkout does not match selected branch commit $env:GITHUB_SHA"
          }

      - name: Record pinned Crafting Storage module revision
        shell: pwsh
        run: |
          $commit = git -C modules/mod-crafting-storage rev-parse HEAD
          if ($LASTEXITCODE -ne 0) { throw "could not resolve pinned mod-crafting-storage commit" }
          "CRAFTING_STORAGE_COMMIT=$commit" | Out-File -FilePath $env:GITHUB_ENV -Append -Encoding utf8
          Write-Host "Using pinned mod-crafting-storage $commit"

      - name: Record pinned Crafting Orders module revision
        shell: pwsh
        run: |
          $commit = git -C modules/mod-crafting-orders rev-parse HEAD
          if ($LASTEXITCODE -ne 0) { throw "could not resolve pinned mod-crafting-orders commit" }
          "CRAFTING_ORDERS_COMMIT=$commit" | Out-File -FilePath $env:GITHUB_ENV -Append -Encoding utf8
          Write-Host "Using pinned mod-crafting-orders $commit"

      - name: Archive name
        shell: pwsh
        run: |
          $sha = (git rev-parse --short HEAD)
          "GITHUB_SHORT_SHA=$sha" | Out-File -FilePath $env:GITHUB_ENV -Append -Encoding utf8
          "ARCHIVE_NAME=windrunner-wow-windows-server-$sha" | Out-File -FilePath $env:GITHUB_ENV -Append -Encoding utf8
          "SQL_ARCHIVE_NAME=windrunner-wow-sql-$sha" | Out-File -FilePath $env:GITHUB_ENV -Append -Encoding utf8

      - name: Bundle SQL
        shell: pwsh
        run: |
          $root = $env:GITHUB_WORKSPACE
          $createDb = Join-Path $root 'sql/create_databases.sql'
          $baseDir = Join-Path $root 'sql/base'
          if (-not (Test-Path $createDb)) { throw "Required SQL missing: sql/create_databases.sql" }
          if (-not (Test-Path $baseDir)) { throw "Required SQL missing: sql/base/" }

          $sqlRoot = Join-Path $root 'sql-bundle/sql'
          New-Item -ItemType Directory -Force -Path $sqlRoot | Out-Null

          Copy-Item $createDb (Join-Path $sqlRoot 'create_databases.sql')
          Copy-Item -Recurse $baseDir (Join-Path $sqlRoot 'base')

          $dbUpdates = Join-Path $root 'sql/database_updates'
          if (Test-Path $dbUpdates) {
            Copy-Item -Recurse $dbUpdates (Join-Path $sqlRoot 'database_updates')
          } else {
            Write-Warning 'sql/database_updates/ missing; bundle will omit migration SQL'
          }

          $pbWorld = Join-Path $root 'src/modules/PlayerBots/sql/world'
          $pbChars = Join-Path $root 'src/modules/PlayerBots/sql/characters'
          $pbOk = (Test-Path $pbWorld) -and (Test-Path $pbChars)
          if (-not $pbOk) {
            if (-not (Test-Path $pbWorld)) { Write-Warning "PlayerBots world SQL missing: $pbWorld" }
            if (-not (Test-Path $pbChars)) { Write-Warning "PlayerBots characters SQL missing: $pbChars" }
          } else {
            $pbDestWorld = Join-Path $sqlRoot 'playerbots/world'
            $pbDestChars = Join-Path $sqlRoot 'playerbots/characters'
            New-Item -ItemType Directory -Force -Path $pbDestWorld, $pbDestChars | Out-Null
            Copy-Item -Recurse (Join-Path $pbWorld '*') $pbDestWorld
            Copy-Item -Recurse (Join-Path $pbChars '*') $pbDestChars
          }

          @(
            "commit=$env:GITHUB_SHA"
            "short=$env:GITHUB_SHORT_SHA"
            "ref=$env:GITHUB_REF"
            "mod_crafting_storage_commit=$env:CRAFTING_STORAGE_COMMIT"
            "mod_crafting_orders_commit=$env:CRAFTING_ORDERS_COMMIT"
          ) | Set-Content (Join-Path $sqlRoot 'BUILD_INFO.txt')

          $zip = Join-Path $root "$env:SQL_ARCHIVE_NAME.zip"
          if (Test-Path $zip) { Remove-Item $zip -Force }
          Compress-Archive -Path (Join-Path $root 'sql-bundle/*') -DestinationPath $zip
          "SQL_ZIP_PATH=$zip" | Out-File -FilePath $env:GITHUB_ENV -Append -Encoding utf8

      - name: Cache vcpkg
        uses: actions/cache@v4
        with:
          path: |
            C:/vcpkg/installed
            C:/vcpkg/downloads
            C:/vcpkg/buildtrees
            C:/vcpkg/packages
          key: vcpkg-win-ace-boost-${{ hashFiles('.github/workflows/windows-build.yml') }}
          restore-keys: |
            vcpkg-win-ace-boost-

      - name: Bootstrap vcpkg
        shell: pwsh
        run: |
          if (-not (Test-Path "$env:VCPKG_ROOT/vcpkg.exe")) {
            git clone --depth 1 https://github.com/microsoft/vcpkg.git $env:VCPKG_ROOT
            & "$env:VCPKG_ROOT/bootstrap-vcpkg.bat" -disableMetrics
          }

      - name: ACE + Boost
        shell: pwsh
        run: |
          # No CMAKE_TOOLCHAIN_FILE — dep/windows owns OpenSSL/MySQL; toolchain mixes 3.x in.
          $pkgs = @(
            'ace:x64-windows',
            'boost-algorithm:x64-windows',
            'boost-asio:x64-windows',
            'boost-bimap:x64-windows',
            'boost-bind:x64-windows',
            'boost-filesystem:x64-windows',
            'boost-functional:x64-windows',
            'boost-smart-ptr:x64-windows',
            'boost-stacktrace:x64-windows',
            'boost-thread:x64-windows',
            'boost-system:x64-windows'
          )
          & "$env:VCPKG_ROOT/vcpkg.exe" install @pkgs --disable-metrics
          if ($LASTEXITCODE -ne 0) { throw "vcpkg install failed" }

      - name: Configure
        shell: pwsh
        run: |
          $v = "$env:VCPKG_ROOT/installed/x64-windows"
          cmake -B build -A x64 `
            -DCMAKE_BUILD_TYPE=$env:BUILD_TYPE `
            -DCMAKE_INSTALL_PREFIX="$env:INSTALL_DIR" `
            -DBUILD_PLAYERBOTS=ON `
            -DMODULE_MOD_DUNGEON_CLEAR=static `
            -DUSE_EXTRACTORS=ON `
            -DALLOW_TURTLE_ADDONS=ON `
            -DACE_ROOT="$v" `
            -DBOOST_ROOT="$v" `
            -DBoost_NO_BOOST_CMAKE=ON
          if ($LASTEXITCODE -ne 0) { throw "configure failed" }

      - name: Build
        shell: pwsh
        run: |
          cmake --build build --config $env:BUILD_TYPE --parallel 1
          if ($LASTEXITCODE -ne 0) { throw "build failed" }

      - name: Install
        shell: pwsh
        run: |
          cmake --install build --config $env:BUILD_TYPE
          if ($LASTEXITCODE -ne 0) { throw "install failed" }

      - name: Runtime DLLs
        shell: pwsh
        run: |
          $bin = "$env:VCPKG_ROOT/installed/x64-windows/bin"
          $dest = $env:INSTALL_DIR
          Copy-Item -Force "$bin/ACE.dll" $dest
          Copy-Item -Force "$bin/boost_*.dll" $dest

          foreach ($name in @(
              'mangosd.exe', 'realmd.exe',
              'mangosd.conf', 'realmd.conf', 'mods.conf', 'rate.conf',
              'aiplayerbot.conf', 'ahbot.conf')) {
            if (-not (Test-Path (Join-Path $dest $name))) {
              throw "missing $name in install dir"
            }
          }
          $moduleConf = @(Get-ChildItem -Path (Join-Path $dest 'modules') -Filter '*.conf' -File -ErrorAction SilentlyContinue)
          if ($moduleConf.Count -lt 1) {
            throw 'no module configs were installed'
          }
          $stale = @(Get-ChildItem -Path $dest -Recurse -Filter '*.conf.dist' -File -ErrorAction SilentlyContinue)
          if ($stale.Count -gt 0) {
            throw ("config templates were installed without renaming: " + (($stale | ForEach-Object { $_.FullName }) -join ', '))
          }

          @(
            "commit=$env:GITHUB_SHA"
            "short=$env:GITHUB_SHORT_SHA"
            "ref=$env:GITHUB_REF"
            "mod_crafting_storage_commit=$env:CRAFTING_STORAGE_COMMIT"
            "mod_crafting_orders_commit=$env:CRAFTING_ORDERS_COMMIT"
          ) | Set-Content (Join-Path $dest 'BUILD_INFO.txt')

      - name: Zip
        shell: pwsh
        run: |
          $zip = Join-Path $env:GITHUB_WORKSPACE "$env:ARCHIVE_NAME.zip"
          if (Test-Path $zip) { Remove-Item $zip -Force }
          Compress-Archive -Path (Join-Path $env:INSTALL_DIR '*') -DestinationPath $zip
          "ZIP_PATH=$zip" | Out-File -FilePath $env:GITHUB_ENV -Append -Encoding utf8

      - name: Create GitHub Release with build archives
        shell: pwsh
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          RELEASE_NAME: ${{ inputs.release_name }}
        run: |
          if ([string]::IsNullOrWhiteSpace($env:RELEASE_NAME)) {
            throw 'Release name must not be empty'
          }
          foreach ($path in @($env:ZIP_PATH, $env:SQL_ZIP_PATH)) {
            if (-not (Test-Path -LiteralPath $path -PathType Leaf)) {
              throw "Release archive missing: $path"
            }
          }

          $prefix = $env:RELEASE_VERSION
          $pattern = '^' + [regex]::Escape($prefix) + '\.([0-9]+)$'
          $tags = gh release list --repo $env:GITHUB_REPOSITORY --limit 1000 --json tagName --jq '.[].tagName'
          if ($LASTEXITCODE -ne 0) { throw 'Could not list existing releases' }
          $highest = 0
          foreach ($existingTag in @($tags)) {
            if ($existingTag -match $pattern) {
              $patch = [int]$Matches[1]
              if ($patch -gt $highest) { $highest = $patch }
            }
          }
          $tag = "$prefix.$($highest + 1)"
          gh release create $tag $env:ZIP_PATH $env:SQL_ZIP_PATH --repo $env:GITHUB_REPOSITORY --target $env:GITHUB_SHA --title $env:RELEASE_NAME --generate-notes
          if ($LASTEXITCODE -ne 0) { throw "Failed to create release $tag" }
